Hello {{ user.get_full_name|default:user.get_username }},

We received a request to reset the password for your AeroReserve account.

Use the link below to choose a new password:

{{ protocol }}://{{ domain }}{% url 'reservations:password_reset_confirm' uidb64=uid token=token %}

If you did not request this password reset, you can safely ignore this email.

AeroReserve